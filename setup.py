from setuptools import setup

setup(name='gym_trade',
      version='0.0.1',
      install_requires=['gym',
                        'numpy',
                        'matplotlib']  # And any other dependencies hsr needs
)